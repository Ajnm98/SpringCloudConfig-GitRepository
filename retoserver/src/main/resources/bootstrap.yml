server:
  port: 8888
spring:
# Spring Cloud Config Server Repository
  cloud:
    config:
     server:
        git:
         uri: https://github.com/Ajnm98/SpringCloudConfig-GitRepository

        search-paths: config-data
        default-label: master


  security:
    user:
      name: root
      password: s3cr3t